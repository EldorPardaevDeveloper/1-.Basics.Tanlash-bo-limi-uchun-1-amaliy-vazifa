
//Telefon

 int oldStr = Convert.ToInt32(Console.ReadLine());
 
 string oldMessage =
     7 >= oldStr
        ? "telefon"
        : "TELEFON";

        System.Console.WriteLine(oldMessage);


 //Kompyuter
 int str = Convert.ToInt32(Console.ReadLine());
 
 string message =
     8 >= str
        ? "kompyuter"
        : "KOMPYUTER";

        System.Console.WriteLine(message);
